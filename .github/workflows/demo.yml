name: Python CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Creating OGC API Features Deployment 🔧
      run: |
         sudo apt-get install python3 python3-setuptools python3-pip python3-dev  -y
         sudo pip3 install rdflib geopandas shapely pyshp geodatasets geojson_rewind
         python3 src/rdfconverter.py
    - name: Deploy 🚀
      uses: JamesIves/github-pages-deploy-action@releases/v3
      with:
         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
         BRANCH: gh-pages
         FOLDER: result/